public interface Transformer<A, B> {
    B convertFrom(A from);
    (return.....)
}
//how come thee convertFrom?
functional interface-only one method
lambda expression-easier function
isfraud ->isfraud = = boolean convertfrom(boolean isfraud)

concurrentmodification - for each loop cant remove